import s from './TableContainer.module.scss';

export const TableContainer = ({ activeTableId, tablesConfigArray, wordJson }) => {
  return <div className={s['table-container']}>Table Container</div>;
};
